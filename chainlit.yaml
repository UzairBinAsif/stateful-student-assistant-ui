chainlit: 2.0.0

ui:
  name: "Uzair's Assistant Chatbot"
  description: "A simple chatbot powered by Gemini LLM"

deafult_expand_messages: true

auth:
  required: true
  providers:
    - github
    - google

oauth_providers:
  github:
    client_id: ${OAUTH_GITHUB_CLIENT_ID}
    client_secret: ${OAUTH_GITHUB_CLIENT_SECRET}
  google:
    client_id: ${OAUTH_GOOGLE_CLIENT_ID}
    client_secret: ${OAUTH_GOOGLE_CLIENT_SECRET}